import type { EmotionalRegister } from './types.js'

// Detects user's emotional register from voice transcript or text.
// Stub — replaced with tone analysis in Phase 5.
export function detectEmotionalRegister(_input: string): EmotionalRegister {
  return 'operational'
}